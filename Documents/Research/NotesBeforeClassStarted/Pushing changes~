*git add .
*git commit -m 'message'
*git push origin nameOfBranchHere
-------------------------------------------------

time to switch branches?
First commit to your branch. Then...
*git checkout master
*git merge branchName
*git push origin master
*git checkout -b 'newBranch'











New file?
git add <nameOfFile>

git add -u


git commit -m 'message'
git push -u




make and switch to new branch
git checkout -b 'nameHere'

this branch is now your 'head' or where your commits will go

switch between branches
git checkout branchName
i.e. git checkout master OR git checkout createRecipes

Merge branch into master
git checkout master
git merge branchName
