<%= form_for @recipe do |f| %>
  <% if @recipe.errors.any? %>

  <div class="error_explanations">
   <h2>The following errors are keeping this recipe from being saved <h2>
   <h3><%= pluralize(@recipe.errors.count, "error") %> </h3>
   <ul>
    <% @recipe.errors.full_messages.each do |m| %>
      <li><%= m %></li>
    <%end%>
   </ul>
  </div>
<% end %>

  <p>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title %>
  </p>

<%-#ingredients will go here. It will be displayed as a drop down list of foods in the database. JS will need to be used so that every time they pick something on a drop down, another one pops up underneath it until they get all of their ingredients. The RecipesIngredients table will need to be used for this-%>

  <p>
    <%= f.label :directions, "Directions" %>
    <%= f.text_area  :directions %>
  </p>

  <p>
    <%= f.label :cookTime, "Cook time(in hours please)" %>
    <%= f.text_field :cookTime %>
  </p>

  <p>
    <%= f.label :costOfIngredients, "Estimated cost of ingredients(decimal please)" %>
    <%= f.text_field :costOfIngredients %>
  </p>

  <p>
    <%= f.label :canPrepareAhead, "Can this be prepared ahead of time and thrown into the oven right before dinner?" %>
    <%= f.check_box :canPrepareAhead %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<%end%>
