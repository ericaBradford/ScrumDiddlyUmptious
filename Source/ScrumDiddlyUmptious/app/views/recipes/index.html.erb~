<div id="facets">
  <h3>Rating</h3>
  <ul>
    <% for row in @search.facet(:average_rating).rows %>
      <li>
        <% if params[:rating].blank? %>
          <%= link_to row.value, :rating => row.value %> (<%= row.count %>)
        <% else %>
          <strong><%= row.value %></strong> (<%= link_to "remove", :rating => nil %>)
        <% end %>
      </li>
    <% end %>
  </ul>
</div>



<%- model_class = Recipe -%>
<div class="recipes">
  <% @recipes.each do |recipe| %>
    <div class="recipe">
      <div class="recipeContents">
        <h4><%= link_to recipe.title, recipe_path(recipe) %></h4>
        <%= image_tag recipe.picture.url(:thumb) %>
        <%= rating_for recipe, "Rating", :disable_after_rate => false %>
      </div>
    </div>
  <% end %>
</div>
<div class="clearfix"/>

<%= will_paginate @recipes, :container => false, :params => { :search_text => params[:search_text]} %>

<% if user_signed_in? %>
  <%= link_to t('.new', :default => t("helpers.links.new")),
            new_recipe_path,
            :class => 'btn btn-default', data: {no_turbolink: true}  %>
<% end %>


<%= debug @searchResults %>
